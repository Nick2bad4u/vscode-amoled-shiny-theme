{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Theme Extension in Insiders",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
